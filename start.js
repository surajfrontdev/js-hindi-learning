const userid = 83730292
let userName = "suraj Verma"
var EmailId = "suraj@gmail.com"

console.table([userid, userName, EmailId])

/*
var can be change
let can be assing change you can assing only once
const is fixed in every aspect noting can be change
*/

console.log(userid)